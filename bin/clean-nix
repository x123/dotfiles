#!/usr/bin/env bash
set -xeuo pipefail

nix-store --gc -v
nix-store --optimise
# consider running this if inodes a problem
#nix-collect-garbage -d
echo "Consider running: home-manager expire-generations \"-30 days\""
